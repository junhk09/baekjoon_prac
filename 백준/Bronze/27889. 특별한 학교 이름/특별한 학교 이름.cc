#include <iostream>
#include <algorithm>
#include <stack>
#include <vector>
#include <string>
using namespace std;
int main() {
    string s;
    cin >> s;
    if (s[0] == 'N') cout << "North London Collegiate School";
    if (s[0] == 'B') cout << "Branksome Hall Asia";
    if (s[0] == 'K') cout << "Korea International School";
    if (s[0] == 'S') cout << "St. Johnsbury Academy";
  
    
}