#include <iostream>
#include <algorithm>
#include <string>
#include <stack>
using namespace std;
int main() {
	int n;
	cin >> n;
	if (n == 0) cout << "YONSEI";
	else cout << "Leading the Way to the Future";
}